<div class="gallery" id="gallery">
  {{#each fuzzles}}
    <div class="gallery-item">
      <div class="content">
        <img src="/assets/images/{{file}}" title="{{title}}" />
        <div class="tags">{{tags}}</div>
      </div>
    </div>
  {{/each}}
</div>

